{
  "scripts": {
    "setup": "ruby bin/setup\nnpm install",
    "run": "powershell -NoProfile -ExecutionPolicy Bypass -File bin/dev.ps1",
    "archive": "powershell -NoProfile -Command \"Remove-Item -Recurse -Force node_modules, public/vite, public/vite-ssr, public/vite-dev, tmp/cache, tmp/pids, tmp/sockets, tmp/storage -ErrorAction SilentlyContinue; Remove-Item log/*.log -ErrorAction SilentlyContinue\""
  }
}
